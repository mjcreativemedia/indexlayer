---
import ListingLayout from "../../layouts/ListingLayout.astro";
import { getPublishedCollection } from "../../lib/content";

const entries = await getPublishedCollection("guides");
---

<ListingLayout collection="guides" entries={entries} />
