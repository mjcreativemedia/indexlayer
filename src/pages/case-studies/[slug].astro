---
import RedirectLayout from "../../layouts/RedirectLayout.astro";
import { legacyPostRedirects } from "../../lib/legacyRedirects";

export function getStaticPaths() {
  return legacyPostRedirects["case-studies"].map((slug) => ({
    params: { slug },
    props: { slug },
  }));
}

const { slug } = Astro.props;
---

<RedirectLayout from={`/case-studies/${slug}/`} to={`/posts/${slug}/`} />
